sounddevice~=0.5.2
soundfile~=0.12.1
numpy~=2.2.6
pynput~=1.8.1
faster-whisper~=1.1.1
torch~=2.7.1

# Required by faster-whisper (missing dependency)
requests~=2.32.0

# GUI Framework (LGPL license, better for commercial use)
PySide6~=6.7.0

# GNOME system tray support (AppIndicator3)
PyGObject~=3.42.0

# Cloud transcription support
openai~=2.19.0

# Cross-platform clipboard (replaces pyperclip)
pyclip~=0.7.0

# Windows-specific keyboard support
keyboard~=0.13.5; platform_system=="Windows"